# TASK-0002: ディレクトリ構造とプロジェクト初期化

**作成日**: 2025-12-29
**タスクタイプ**: DIRECT
**見積もり工数**: 3時間
**優先度**: P0 (最優先)
**実装者**: TBD
**レビュアー**: TBD

## 概要

プロジェクト全体のディレクトリ構造を構築し、基本的な設定ファイルとスタイルシートを準備します。

**関連要件**: REQ-001~REQ-904 (全体)
**関連設計**: [architecture.md](../../design/blog-article-management/architecture.md), [tech-stack.md](../../tech-stack.md)

## 🎯 達成目標

- [x] 推奨ディレクトリ構造の構築
- [x] グローバルスタイルの設定
- [x] Tailwind CSS設定
- [x] TypeScript設定の確認

## 📋 実装ステップ

### Step 1: ディレクトリ構造の作成 🔵

**信頼性**: 🔵 *tech-stack.md・architecture.mdより*

```bash
# ディレクトリ作成
mkdir -p src/components
mkdir -p src/layouts
mkdir -p src/pages/blog
mkdir -p src/pages/tags
mkdir -p src/utils
mkdir -p src/styles
mkdir -p public/images

# .gitkeepファイルを作成（空ディレクトリをGitで管理）
touch src/components/.gitkeep
touch src/layouts/.gitkeep
touch src/utils/.gitkeep
touch public/images/.gitkeep
```

**成功基準**:
- ✅ すべてのディレクトリが存在する
- ✅ ディレクトリ構造がtech-stack.mdの推奨構成と一致する

---

### Step 2: グローバルスタイルの設定 🔵

**信頼性**: 🔵 *tech-stack.md・architecture.mdより*

**ファイル**: `src/styles/global.css`

```css
/**
 * グローバルスタイル - Tailwind CSS + カスタムスタイル
 * 🔵 信頼性: tech-stack.md・architecture.mdより
 */

@tailwind base;
@tailwind components;
@tailwind utilities;

/**
 * カスタムベーススタイル
 * NFR-301: セマンティックHTML、WCAG 2.1 AA準拠
 */
@layer base {
  :root {
    /* ライトモードカラー */
    --color-primary: 59 130 246; /* blue-500 */
    --color-secondary: 107 114 128; /* gray-500 */
    --color-background: 255 255 255; /* white */
    --color-text: 17 24 39; /* gray-900 */
  }

  .dark {
    /* ダークモードカラー (tech-stack.md: ダークモードデフォルト) */
    --color-primary: 96 165 250; /* blue-400 */
    --color-secondary: 156 163 175; /* gray-400 */
    --color-background: 17 24 39; /* gray-900 */
    --color-text: 243 244 246; /* gray-100 */
  }

  /* NFR-304: フォーカス可視化 (WCAG 2.1 AA) */
  *:focus-visible {
    @apply outline-2 outline-offset-2 outline-blue-500;
  }

  /* セマンティックHTML - 基本スタイル */
  html {
    @apply scroll-smooth;
  }

  body {
    @apply bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100;
    @apply font-sans antialiased;
  }

  /* 見出しスタイル */
  h1, h2, h3, h4, h5, h6 {
    @apply font-bold leading-tight;
  }

  h1 { @apply text-4xl md:text-5xl; }
  h2 { @apply text-3xl md:text-4xl; }
  h3 { @apply text-2xl md:text-3xl; }
  h4 { @apply text-xl md:text-2xl; }
  h5 { @apply text-lg md:text-xl; }
  h6 { @apply text-base md:text-lg; }

  /* リンクスタイル */
  a {
    @apply text-blue-600 dark:text-blue-400 hover:underline;
    @apply transition-colors duration-200;
  }

  /* コードブロックスタイル (REQ-111: Shiki対応) */
  pre {
    @apply rounded-lg overflow-x-auto p-4 my-4;
  }

  code {
    @apply font-mono text-sm;
  }

  /* 画像のレスポンシブ対応 (NFR-202) */
  img {
    @apply max-w-full h-auto;
  }
}

/**
 * カスタムコンポーネントスタイル
 */
@layer components {
  /* ボタンスタイル */
  .btn {
    @apply px-4 py-2 rounded-lg font-medium;
    @apply transition-colors duration-200;
    @apply focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2;
  }

  .btn-primary {
    @apply bg-blue-600 text-white hover:bg-blue-700;
    @apply dark:bg-blue-500 dark:hover:bg-blue-600;
  }

  .btn-secondary {
    @apply bg-gray-200 text-gray-900 hover:bg-gray-300;
    @apply dark:bg-gray-700 dark:text-gray-100 dark:hover:bg-gray-600;
  }

  /* カードスタイル */
  .card {
    @apply bg-white dark:bg-gray-800;
    @apply border border-gray-200 dark:border-gray-700;
    @apply rounded-lg shadow-sm;
    @apply transition-shadow duration-200;
    @apply hover:shadow-md;
  }
}

/**
 * ユーティリティクラス
 */
@layer utilities {
  /* コンテナ最大幅 */
  .container-narrow {
    @apply max-w-3xl mx-auto px-4;
  }

  .container-wide {
    @apply max-w-7xl mx-auto px-4;
  }

  /* テキスト切り詰め */
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}
```

**成功基準**:
- ✅ Tailwind CSSディレクティブが正しく読み込まれる
- ✅ ダークモード変数が定義されている
- ✅ WC AG 2.1 AA準拠のフォーカススタイルが定義されている

---

### Step 3: Tailwind CSS設定の確認 🔵

**信頼性**: 🔵 *tech-stack.mdより*

**ファイル**: `tailwind.config.mjs` (既存ファイルの確認・必要に応じて更新)

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  darkMode: 'class', // tech-stack.md: ダークモードデフォルト
  theme: {
    extend: {
      colors: {
        primary: 'rgb(var(--color-primary) / <alpha-value>)',
        secondary: 'rgb(var(--color-secondary) / <alpha-value>)',
        background: 'rgb(var(--color-background) / <alpha-value>)',
        text: 'rgb(var(--color-text) / <alpha-value>)',
      },
      typography: (theme) => ({
        DEFAULT: {
          css: {
            // Shikiのシンタックスハイライトと統合
            'code::before': {
              content: '""',
            },
            'code::after': {
              content: '""',
            },
          },
        },
      }),
    },
  },
  plugins: [
    require('@tailwindcss/typography'),
  ],
};
```

**成功基準**:
- ✅ `darkMode: 'class'` が設定されている
- ✅ CSS変数がTailwindテーマに統合されている
- ✅ `@tailwindcss/typography` プラグインが設定されている

---

### Step 4: TypeScript設定の確認 🔵

**信頼性**: 🔵 *tech-stack.mdより*

**ファイル**: `tsconfig.json` (既存ファイルの確認)

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@components/*": ["src/components/*"],
      "@layouts/*": ["src/layouts/*"],
      "@utils/*": ["src/utils/*"]
    }
  }
}
```

**成功基準**:
- ✅ `strict: true` が有効
- ✅ パスエイリアスが設定されている
- ✅ `pnpm astro check` がエラーなく通る

---

## 🧪 テスト要件

### 動作確認テスト

**手動確認**:
1. `pnpm dev` でサーバー起動
2. スタイルが正しく適用されることを確認
3. ダークモード切り替えが動作することを確認
4. `pnpm astro check` で型エラーがないことを確認

---

## 🎨 UI/UX要件

このタスクはインフラ設定のため、直接的なUI/UX要件はありませんが、以下を準備します:

### ダークモード対応 🔵

**信頼性**: 🔵 *tech-stack.mdより*

- デフォルトでダークモード対応
- ライト/ダークモード切り替え用のCSS変数を定義

### アクセシビリティ対応 🔵

**信頼性**: 🔵 *NFR-301~304より*

- フォーカス可視化スタイル (WCAG 2.1 AA準拠)
- セマンティックHTML用の基本スタイル
- 十分なコントラスト比を持つカラーパレット

---

## 📦 依存関係

### 前提タスク

- TASK-0001: Content Collections設定とスキーマ定義

### 後続タスク

- TASK-0003: 基本レイアウトの実装
- TASK-0004: ヘッダー・フッターコンポーネントの実装

---

## 🔧 技術仕様

### 使用技術

- **Astro 5.1+** 🔵 *tech-stack.mdより*
- **Tailwind CSS 4.0+** 🔵 *tech-stack.mdより*
- **TypeScript 5.7+** 🔵 *tech-stack.mdより*

### ディレクトリ構成

```
src/
├── components/       # Astroコンポーネント
├── layouts/          # レイアウトコンポーネント
├── pages/            # ルーティングページ
│   ├── blog/         # ブログ関連ページ
│   └── tags/         # タグ関連ページ
├── utils/            # ユーティリティ関数
└── styles/           # グローバルスタイル
    └── global.css    # Tailwind CSS + カスタムスタイル

public/
└── images/           # 静的画像ファイル
```

---

## 📝 実装時の注意事項

### スタイル設計の注意点 🔵

**信頼性**: 🔵 *architecture.md・NFR要件より*

1. **CSS変数の使用**:
   - ライト/ダークモード対応のため、CSS変数でカラーパレットを定義
   - Tailwindテーマに統合して `bg-primary` 等で使用可能に

2. **アクセシビリティ**:
   - フォーカススタイルは必ず可視化（`:focus-visible`）
   - コントラスト比はWCAG 2.1 AA準拠（4.5:1以上）

3. **レスポンシブデザイン**:
   - モバイルファースト設計
   - Tailwindの `md:`, `lg:` ブレークポイントを活用

---

## ✅ 完了条件

### 必須条件

- [x] すべてのディレクトリが作成されている
- [x] `src/styles/global.css` が実装されている
- [x] Tailwind CSS設定が正しく動作する
- [x] TypeScript設定が正しく動作する
- [x] `pnpm astro check` がエラーなく通る

### 品質基準

- [x] CSS変数が正しく定義されている
- [x] ダークモード対応のスタイルが準備されている
- [x] WC AG 2.1 AA準拠のアクセシビリティスタイルが定義されている

---

## 🔄 レビューポイント

### コードレビュー観点

1. **ディレクトリ構造**: tech-stack.mdの推奨構成と一致するか
2. **スタイル定義**: CSS変数、ダークモード、アクセシビリティが適切か
3. **Tailwind設定**: プラグイン、テーマ拡張が正しいか
4. **TypeScript設定**: strictモード、パスエイリアスが正しいか

---

## 📚 参考資料

- [Astro スタイリング公式ドキュメント](https://docs.astro.build/ja/guides/styling/)
- [Tailwind CSS 公式ドキュメント](https://tailwindcss.com/docs)
- **プロジェクト内設計書**:
  - `docs/tech-stack.md`
  - `docs/design/blog-article-management/architecture.md`

---

## 🏷️ タグ・ラベル

`infrastructure`, `styling`, `tailwind`, `typescript`, `p0`

---

## 📊 進捗管理

- **ステータス**: ✅ Completed
- **開始日**: 2025-12-30
- **完了日**: 2025-12-30
- **実績工数**: 0.5時間（見積もり: 3時間）

---

**信頼性レベルサマリー**:
- 🔵 青信号: 100% (設計文書から確実)
- 🟡 黄信号: 0%
- 🔴 赤信号: 0%
